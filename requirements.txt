python-dotenv>=1.0.0
requests>=2.31.0
pandas>=2.0.3
numpy>=1.24.3
coinbase-advanced-py>=1.8.2
# AI Dependencies - Following AI_MODEL_STEERING.md
google-genai>=0.3.0  # NEW unified Google AI/Vertex AI SDK for preview models
google-auth>=2.22.0
schedule>=1.2.0
# Backtesting Dependencies
vectorbt>=0.25.0  # Compatible with Python 3.10
# Using ta library instead of pandas-ta for Python 3.10 compatibility
ta>=0.11.0  # Technical analysis library compatible with Python 3.10
pyarrow>=10.0.0
google-cloud-storage>=2.10.0
numba>=0.58.0
fastparquet>=0.8.3
# python-pushover==0.4  # Using requests directly instead due to compatibility issues
# psutil - removed (was only used for uptime monitoring)
# No longer needed as we're using Python's built-in http.server
# flask==2.0.1
# werkzeug==2.0.1
